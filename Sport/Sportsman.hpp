#pragma once
#include <string>
#include <vector>

using namespace std;

struct Sportsmen
{
	vector<string> name;
	vector<int> age;
	vector<int> height;
	vector<int> weight;
	vector<string> sport;
	vector<string> sportRank;
};